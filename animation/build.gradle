dependencies {
    api project(":core:widgettree")
    api project(":geom") // Interpolator miatt
    implementation project(":graphics") // Faded miatt
    implementation project(":input") // Faded miatt
}